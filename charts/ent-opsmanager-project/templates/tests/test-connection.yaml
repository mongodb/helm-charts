apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ent-opsmanager-project.fullname" . }}-test-connection"
  labels:
    {{- include "ent-opsmanager-project.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ent-opsmanager-project.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

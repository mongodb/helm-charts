apiVersion: v1
kind: Service
metadata:
  name: {{ include "mongodb-connection-tester.fullname" . }}
  labels:
    {{- include "mongodb-connection-tester.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mongodb-connection-tester.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mongodb-connection-tester.fullname" . }}-test-connection"
  labels:
    {{- include "mongodb-connection-tester.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mongodb-connection-tester.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

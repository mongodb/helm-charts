apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "community-operator-crds.fullname" . }}-test-connection"
  labels:
    {{- include "community-operator-crds.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "community-operator-crds.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

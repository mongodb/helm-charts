apiVersion: v1
kind: Service
metadata:
  name: {{ include "atlas-operator.fullname" . }}
  labels:
    {{- include "atlas-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "atlas-operator.selectorLabels" . | nindent 4 }}
